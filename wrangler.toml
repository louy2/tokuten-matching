name = "tokuten-matching"
compatibility_date = "2026-03-15"
pages_build_output_dir = "dist"

# D1 binding — used by vitest for local test database.
# Production binding is configured via Cloudflare Dashboard.
[[d1_databases]]
binding = "DB"
database_name = "tokuten-db"
database_id = "00000000-0000-0000-0000-000000000000"
migrations_dir = "drizzle"

# KV (SESSIONS) binding is configured via the Cloudflare Dashboard
# (Pages > Settings > Functions > Bindings)
