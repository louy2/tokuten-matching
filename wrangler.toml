name = "tokuten-matching"
compatibility_date = "2026-03-15"
pages_build_output_dir = "dist"

# D1 and KV bindings are configured via the Cloudflare Dashboard
# (Pages > Settings > Functions > Bindings)
# to avoid committing resource IDs to the repository.
